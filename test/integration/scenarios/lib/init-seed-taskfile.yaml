version: '3'

tasks:
  docker-login:
    desc: 'Create Docker login secret but only if the var "TEST_CREATE_DOCKER_LOGIN_SECRET" is set.'
    cmds:
    - echo pass

  default:
    cmds:
    - task: docker-login
